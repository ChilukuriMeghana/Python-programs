program to find maximum and minumum freequency numbers and
    print their difference.
------------------------------------------------------------
l=list(map(int,input().split()))
d={}
for i in l:
    if i not in d:
        d[i]=1 
    else:
        d[i]=d[i]+1 
m=list(d.values())
r=max(m)
k=min(m)
print('Difference=',r-k)
--------------------------------------------------
Output:
1 2 2 3 3 3
Difference=  2
--------------------------------------------------
